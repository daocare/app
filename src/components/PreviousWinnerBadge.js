import React from 'react';

const PreviousWinnerBadge = () => (
  <div>
    <div
      style={{
        display: 'inline-block',
        color: 'white',
        borderRadius: '8px',
        backgroundColor: '#6850A8',
        padding: '0.2rem 0.5rem',
        fontFamily: 'nunito',
        fontWeight: 700,
        margin: '1rem 0rem',
      }}
    >
      PREVIOUS WINNER
    </div>
  </div>
);

export default PreviousWinnerBadge;
